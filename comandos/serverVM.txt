Antes de conectar à maquina virtual é preciso dar um 'install ssh' para fazê-lo um servidor.

> Para configurar o SSH da máquina virtual

-sudo nano /etc/ssh/ssh_config
-(Descomentar e negar o valor de PasswordValidation) #problematico

> Para conectar a máquina principal à virtual

Na máquina principal:

-ssh-keygen ....
-(Copio o código encontrando seu arquivo. Está em ...\emers\.ssh)

Na máquina virtual:

-nano ~/.ssh/authorized_keys
-(Colo a chave)

> Instalo o nginx

-sudo apt install nginx